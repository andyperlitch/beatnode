var Model = require('../js/app-model');

describe('app-model.js', function() {
    
	var sandbox;

	beforeEach(function() {
	    sandbox = sinon.sandbox.create();
	});

	afterEach(function() {
	    sandbox.restore();
	});

});