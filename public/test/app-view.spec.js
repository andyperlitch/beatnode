var View = require('../js/app-view');

describe('app-view.js', function() {
    
	var sandbox;

	beforeEach(function() {
	    sandbox = sinon.sandbox.create();
	});

	afterEach(function() {
	    sandbox.restore();
	});

});