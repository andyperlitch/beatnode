<% unless viewer.crate.include?(sound) %>
  <div class=crating>
    <%= form_for :crating, url: cratings_path do |f| %>
      <%= f.hidden_field :sound_id, value: sound.id %>
      <%= f.submit 'Add to crate' %>
    <% end %>
  </div>
<% end %>
