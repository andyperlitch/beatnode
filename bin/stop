#!/bin/sh
f=./tmp/pids/server.pid
if [ -f "$f" ]; then
  _pid=$(cat ./tmp/pids/server.pid 2>&1 /dev/null)
fi

if [ -z "$_pid" ]; then
  echo "No server running."
  exit 1
else
  echo "Shutting down..."
  kill $_pid
  exit 0
fi
